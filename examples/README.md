# Examples

Two saved bakeoff reports — both default-depth (4 candidates · 2 judges + reconcile · adversarial
round). The structure is exactly what the skill produces and saves to `docs/bakeoffs/`.

- [`email-deadline-generation.md`](email-deadline-generation.md) — **a real run** (the one shown in
  the README demo GIF): Message Batches vs keep-sync vs embedding-merge for an email deadline path,
  where the adversarial round flipped the winner after both judges picked the runner-up.
- [`monolith-vs-services.md`](monolith-vs-services.md) — **illustrative** (numbers hand-authored to
  show the report shape, not a live run): a monolith / modular-monolith / services architecture call.
