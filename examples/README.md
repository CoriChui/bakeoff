# Examples

An **illustrative** bakeoff run, showing the shape of the output. (The numbers here are hand-authored
for documentation, not a live run — but the structure is exactly what the skill produces and saves to
`docs/bakeoffs/`.)

- [`monolith-vs-services.md`](monolith-vs-services.md) — a default-depth (4-candidate, 2-judge) run on
  an architecture choice.
